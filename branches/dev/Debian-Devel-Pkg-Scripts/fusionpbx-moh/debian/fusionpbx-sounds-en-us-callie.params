# -*- mode:sh -*-
base="fusionpbx-sounds"
upstream_base="fusionpbx-sounds"
sound_name="US English Callie"
rate="48000"
sound="$(dpkg-parsechangelog -lchangelog | grep '^Source' | awk '{print $2}' | sed -e "s/${base}-//")"
upstream_sound="$sound"
path="$(echo "$sound" | sed -e 's:-:/:g')"
